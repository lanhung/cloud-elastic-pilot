{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "hooke.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: hooke
    app.kubernetes.io/instance: {{ .Release.Name }}
automountServiceAccountToken: true
{{- end }}
